import numpy as np
import clib



def gaussian2d(h, a, dx, dy, fwhm, nx, ny):
    arr2d = clib.gaussian_array2d(float(h), float(a), float(dx), float(dy),
                                  float(fwhm), int(nx), int(ny))

